extends ./template

block content
  section.sg-components
    h2 Colors
    .sg-color-swatches
      for hex, name in colors
        .sg-color-swatch( style="background-color: #{hex};" )
          = name
          br
          = hex
