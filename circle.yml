checkout:
  post:
    - nvm install && nvm alias default node

dependencies:
  override:
    - yarn

test:
  override:
    - yarn lint
    - yarn type-check
    - yarn test

deployment:
  demo:
    branch: master
    commands:
      - yarn deploy-storybook
