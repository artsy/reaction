machine:
  node:
    version: 7

dependencies:
  override:
    - yarn

test:
  override:
    - yarn test
  post:
    - yarn lint

deployment:
  demo:
    branch: master
    commands:
      - yarn deploy-storybook
