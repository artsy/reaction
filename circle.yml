machine:
  node:
    version: 7

dependencies:
  override:
    - yarn

test:
  override:
    - yarn lint
    - yarn type-check
    - yarn test

deployment:
  demo:
    branch: master
    commands:
      - yarn deploy-storybook
