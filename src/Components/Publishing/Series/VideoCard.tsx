import React, { Component  } from "react"
import styled, { StyledFunction } from "styled-components"

interface Props {
  article: any,
  series: any
}

export class VideoCard extends Component<Props, null> {

  render () {
    return (
      <div>Hello</div>
    )
  }
}