import { Row } from "react-styled-flexboxgrid"
import styled from "styled-components"

// TODO: switch to em once system is established
export const MaxRow = styled(Row)`
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 12px;
`
